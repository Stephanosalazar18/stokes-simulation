# openspec/config.yaml
# Auto-generated by sdd-init for stokes_simulator
schema: spec-driven

context: |
  Tech stack: Vite + TypeScript + Three.js + GLSL custom shaders (planned)
  Architecture: TBD — browser-based Stokes theorem visualizer with three modes
    (2D flow lines, 3D flow with gradients, image-based deformation)
  Testing: none detected yet (planned: Vitest)
  Style: TypeScript strict mode, modular shader files

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
